local servers = {
  "tsserver",    -- TypeScript/JavaScript
  "eslint",      -- ESLint
  --"pyright",     -- Python
}

local server_settings = {}

return {
  servers = servers,
  server_settings = server_settings,
}
